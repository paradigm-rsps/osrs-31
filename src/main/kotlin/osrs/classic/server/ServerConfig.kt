package osrs.classic.server

import java.awt.Point

object ServerConfig {
    object DEFAULTS {
        object HOME_TILE {
            val X = 3121
            val Y = 3121
            val PLANE = 0
        }
    }
    val LOGIN_QUEUE_THREADS = 4
}
